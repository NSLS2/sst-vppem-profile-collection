name: Build and Push VPPEM Container Images

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set IMAGE_NAME for GHCR
      shell: bash
      run: echo "IMAGE_NAME=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Docker meta for vppem-bluesky
      id: meta-vppem-bluesky
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-bluesky
        tags: |
          type=raw,value=latest
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha

    - name: Build and push vppem-bluesky image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./images/bluesky/Containerfile
        push: true
        tags: ${{ steps.meta-vppem-bluesky.outputs.tags }}
        labels: ${{ steps.meta-vppem-bluesky.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Docker meta for vppem-queueserver
      id: meta-vppem-queueserver
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-queueserver
        tags: |
          type=raw,value=latest
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha

    - name: Build and push vppem-queueserver image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./images/queueserver/Containerfile
        push: true
        tags: ${{ steps.meta-vppem-queueserver.outputs.tags }}
        labels: ${{ steps.meta-vppem-queueserver.outputs.labels }}
        build-args: |
          BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-bluesky:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Docker meta for vppem-gui
      id: meta-vppem-gui
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-gui
        tags: |
          type=raw,value=latest
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha

    - name: Build and push vppem-gui image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./images/gui/Containerfile
        push: true
        tags: ${{ steps.meta-vppem-gui.outputs.tags }}
        labels: ${{ steps.meta-vppem-gui.outputs.labels }}
        build-args: |
          BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-bluesky:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Docker meta for vppem-sim
      id: meta-vppem-sim
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-sim
        tags: |
          type=raw,value=latest
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha

    - name: Build and push vppem-sim image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./images/sim/Containerfile
        push: true
        tags: ${{ steps.meta-vppem-sim.outputs.tags }}
        labels: ${{ steps.meta-vppem-sim.outputs.labels }}
        build-args: |
          BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/vppem-bluesky:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

